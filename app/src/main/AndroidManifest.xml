<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SikamdisApp"
        tools:targetApi="31">
        <activity
            android:name=".RiwayatPasienActivity"
            android:exported="false" />
        <activity
            android:name=".LaporanActivity"
            android:exported="false" />
        <activity
            android:name=".ObatActivity"
            android:exported="false" />
        <activity
            android:name=".Rekam_medisActivity"
            android:exported="false" />
        <activity
            android:name=".Activity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false" /> <!-- Aktivitas SplashScreen dengan Intent Filter untuk LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Aktivitas Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" /> <!-- Aktivitas MainActivity (diperbaiki dari 'activitys' menjadi 'activity') -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>